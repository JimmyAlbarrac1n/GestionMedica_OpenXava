package com.gestionmedica.gestionmedica.modelo.enums;

public enum EstadoMedico {
    ACTIVO,      // Médico disponible para atender
    INACTIVO     // Temporalmente no disponible (vacaciones, licencia)
}